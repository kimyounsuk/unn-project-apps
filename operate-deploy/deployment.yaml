apiVersion: apps/v1
kind: Deployment
metadata:
  labels:
    app: nginx
  name: testoprv7
spec:
  replicas: 2
  selector:
    matchLabels:
      app: nginx
  strategy: {}
  template:
    metadata:
      labels:
        app: nginx
    spec:
      containers:
      - name: nginx11
        image: harbor.mycompany.local/unn-project/webv13:latest
        imagePullPolicy: Always
      imagePullSecrets:
      - name: harbor-secret
